	pipeline {
    agent any
    stages {
        stage('Build'){
             steps {
                 sh script: '''
                     echo "I am in Test step"
                     chmod +x ./gradlew
                     ./gradlew build
                 ''',
                 label: "Building Project & Testing"
             }
        }
    }
}
